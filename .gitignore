# Create .gitignore if it doesn't exist
New-Item -ItemType File -Name ".gitignore"

# Add these lines to .gitignore (open in VS Code or Notepad)
node_modules/
.env
.DS_Store
npm-debug.log
*.env
config/local.js
.env.local

node_modules
.next
.env*